// Imports the Home component
import Home from "./components/Home";

const App = () => {
  // Functional component representing the root of the React application
  return <Home />;
  // Renders the Home component inside the App
};

// Exports the component as the default export
export default App;
